# Postgres connection string (same DB the Node app / Render Postgres uses).
# Render: copy the "External Database URL" from your Postgres instance.
DATABASE_URL=postgres://user:password@host:5432/dbname
